# Oriflamme Game Simulation

This is a small playground for reinforcement learning, based on the Oriflamme board game.


## Getting Started

Build local extension, without installing:

```
python setup.py build_ext --inplace
```

Run unit tests:

```
python -m pytest
```
